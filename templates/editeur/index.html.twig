{% extends 'base.html.twig' %}


{% block head %}{% endblock %}

{% block content %}
	<ul>
		{% for editeur in editeurs %}

			<a href="{{ path('serie', {'editeur': editeur.id}) }}">
				<div class=" group cursor-pointer rounded-lg p-4 border border-gray-200	hover:bg-light-blue-500
							hover:border-transparent hover:shadow-lg">
					<div class="text-xl text-center  font-bold">
						{{ editeur.nom }}
					</div>
				</div>
			</a>
		{% endfor%}
	</ul>
{% endblock %}
